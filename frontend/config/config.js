/**
 * The global app config
 *
 * @public
 * @type {Object}
 * @const
 */
const config = {

  /**
   * Full URL to the PHP API
   *
   * @public
   * @property {String} API_URL the url
   */
  API_URL: 'http://blog.dev/api/v1',

  /**
   * Do we need to append a hash to url /#/posts instead
   * of /posts
   *
   * @public
   * @property {Boolean} HASH use hashes?
   */
  HASH: true

}
